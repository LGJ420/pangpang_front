import BasicNoHeightLayout from "../layouts/BasicNoHeightLayout";

const TestPage = () => {

    return (

        <BasicNoHeightLayout>
            <section>

            </section>
        </BasicNoHeightLayout>

    );
}

export default TestPage;